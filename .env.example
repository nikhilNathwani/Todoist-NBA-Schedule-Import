# Todoist OAuth credentials (get from https://developer.todoist.com/appconsole.html)
CLIENT_ID="your-client-id-here"
CLIENT_SECRET="your-client-secret-here"

# OAuth security
STATE_SECRET="random-string-for-csrf-protection"

# Cookie encryption (generate with: openssl rand -hex 32)
ENCRYPTION_KEY="your-64-character-hex-string-here"
COOKIE_SECRET="random-string-for-cookie-signing"

# OAuth redirect URI
# PRODUCTION: https://nba-todoist-import.vercel.app/api/auth/callback
# LOCAL: http://localhost:3000/api/auth/callback
# NOTE: Update this in your Todoist app settings: https://developer.todoist.com/appconsole.html
REDIRECT_URI="http://localhost:3000/api/auth/callback"

# Error-handling demo mode (optional, defaults to disabled)
# When "true", /configure-import?mockTodoistError=<code> and the import
# button on that page will simulate a Todoist API failure (400/401/403/404/
# 429/500/503/network) instead of making a real API call, so the app's
# error-classification and messaging (app/utils/todoistErrors.js) can be
# demoed without needing to actually trigger those states against the real
# Todoist API. Leave unset/false on real deployments; only enable
# deliberately (e.g. locally, or temporarily on Vercel) for a demo.
ENABLE_ERROR_DEMO="false"
